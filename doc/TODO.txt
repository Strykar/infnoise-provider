================================================================================
        Infinite Noise TRNG — OpenSSL 3.x Provider: TODO
================================================================================

Deferred work.  Not blocking alpha; worth doing before beta.

Packaging
  [ ] AUR package (infnoise-provider-git and/or versioned)
  [ ] examples/ — nginx OPENSSL_CONF snippet
  [ ] examples/ — Python cryptography via OpenSSL
  [ ] examples/ — systemd drop-in scoping provider to a single service
  [ ] Signed git tags (git tag -s)
  [ ] Signed release tarballs + PGP fingerprint in README

Documentation
  [ ] doc/FAQ.txt (Provider vs. ENGINE, vs. /dev/hwrng, TLS speed, daemon coexistence)
  [ ] README sweep once upstream #119 lands and libinfnoise tags a release:
      pin minimum libinfnoise version in Requirements, drop the two
      "Known limitations" bullets (library-level single-instance, exit(1)),
      delete the INFNOISE_PATCHED compile-time bridge from src/

CI and testing
  [x] GitHub Actions: CodeQL (security-and-quality) on push + PR + weekly
  [x] GitHub Actions: GCC + Clang build matrix
  [x] GitHub Actions: ASan + UBSan sanitizer builds
  [x] GitHub Actions: cppcheck on push/PR
  [x] GitHub Actions: Coverity Scan (weekly cron)
  [x] GitHub Actions: Codecov coverage upload
  [x] GitHub Actions: OpenSSF Scorecard
  [ ] CIFuzz / OSS-Fuzz integration — write libFuzzer harness against
      dispatch and parameter surface, then apply for OSS-Fuzz onboarding
      and add the CIFuzz action for per-PR short fuzz runs

Release discipline
  [ ] CHANGELOG.md (Keep-a-Changelog) once out of alpha
  [ ] First signed Release, gated on:
      [x] 24 h endurance run green (2026-04-24: 14698 generate calls,
          4.21 GiB, 49.9 KiB/s, 0 errors, 95 churn cycles, RSS +1.2 MiB
          peak, ended -1.0 MiB below start; see issue #8 and README)
      [ ] upstream waywardgeek/infnoise#119 resolved
      [ ] external review

================================================================================
