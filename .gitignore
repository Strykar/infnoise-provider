*.o
*.so
test_infnoise_prov
test_infnoise_prov-asan
test_infnoise_prov-ubsan
test_infnoise_soak

# Generated manpage (built from the .md source via `make man`)
docs/OSSL_PROVIDER-infnoise.7

# libFuzzer reproducers (written to CWD when -artifact_prefix is unset)
crash-*
timeout-*
oom-*
slow-*
leak-*

# LLVM coverage runtime output
*.profraw
*.profdata

# TSan / allocator-failure test binaries
test_infnoise_tsan
test_infnoise_alloc

# libFuzzer harness binaries (built artifacts; sources are tracked)
tests/fuzz/fuzz_dispatch
tests/fuzz/fuzz_ossl_params
tests/fuzz/fuzz_spill_oracle
tests/fuzz/fuzz_provider_init

# Internal development notes — never upload
bg.md

# SBOM (generated at release time via 'make sbom')
sbom.spdx.txt

# Mull mutation testing build artifacts
tests/mutation/runner

# Internal working notes (not for upstream)
notes/
cov/
